# Real inventory and host-specific variables may contain IPs or secrets.
inventory.ini
group_vars/vps.yml

# Real app-stack secrets and generated env files live only on the VPS.
compose/.env

# Consolidated local record of every generated password/secret.
credentials.txt

# Ansible runtime files
*.retry
.ansible/

# Local editor and OS metadata
.DS_Store
.idea/
.vscode/

# Local agent permissions and commands
.claude/

go.sh
