{
	admin 0.0.0.0:2019
	metrics
}

# admin.techmeout.it (the original `medusa` service) is deliberately not
# routed here - kept running as the template new shops get scaffolded from
# (see compose/NEW_TENANT.md), not as a publicly reachable store itself.
# shop.techmeout.it was never routed here at all - it's Vercel-hosted, not
# this host's concern.

n8n.openempower.com {
	reverse_proxy n8n:5678
}

monitor.openempower.com {
	reverse_proxy grafana:3000
}

admin-puntofeste.techmeout.it {
	reverse_proxy puntofeste-backend:9000
}

admin-smoothclothingbrand.techmeout.it {
	reverse_proxy smoothclothingbrand-backend:9000
}

postiz.openempower.com {
	reverse_proxy postiz:5000
}

seo.openempower.com {
	basic_auth {
		luca {$OPEN_SEO_BASIC_AUTH_HASH}
	}
	reverse_proxy open-seo:3001
}
